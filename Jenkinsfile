pipeline {

    agent any

    stages {

        stage('Pipeline Test') {

            steps {

                sh 'hostname'
                sh 'whoami'
                sh 'pwd'

            }
        }

    }
}
